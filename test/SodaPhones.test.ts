import { expect } from 'chai';
import { ethers } from 'hardhat';
import { SodaPhones } from '../typechain';

describe('SodaPhones', () => {
  it('should work', async () => {
    await expect(true).to.eql(true);
  });
});
